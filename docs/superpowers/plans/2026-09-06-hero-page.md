# Hero Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the React recreation of the reference site's hero screen (navbar, headline, CTAs, background marquee, scroll cue, social links) with placeholder content and GSAP-driven animation, matching layout/motion — not the original's identity or assets.

**Architecture:** Vite + React 18, CSS Modules per component, GSAP (+`@gsap/react`'s `useGSAP`, `ScrollTrigger`) for animation. All copy/data lives in one `src/content/site.js` module so components stay presentational. Structural/logic pieces (data shape, hooks, toggle state, rendered content) get automated tests; GSAP timelines get explicit manual-verification steps instead of fake assertions, per the spec.

**Tech Stack:** React 18, Vite 5, gsap 3, @gsap/react 2, Vitest + React Testing Library + jsdom, plain CSS Modules, Google Fonts (Inter, Playfair Display).

**Spec:** `docs/superpowers/specs/2026-09-06-hero-page-design.md`

## Global Constraints

- No TypeScript — plain JS/JSX, matching the spec's stated stack.
- No real names/photos/logos from the reference site — placeholder persona "Alex Rivera" and CSS-gradient placeholder blocks only.
- Fonts: **Inter** (body/headline) and **Playfair Display Italic** (accent words "designer,"/"developer.") via Google Fonts — not the original's PP Mori.
- All copy/data (nav items, social links, headline text, marquee text, CTA labels) lives in `src/content/site.js` — no copy hardcoded inline in components.
- `prefers-reduced-motion: reduce` must disable the marquee loop, scroll-cue bounce, hero parallax, and nav-open scatter animation.
- `html`/`body` use `overflow-x: clip` — no horizontal scroll regardless of decorative elements.
- No automated tests for GSAP timelines/animation itself (per spec, "no meaningful assertions to write against GSAP timelines") — those get manual browser-verification steps. Everything else (data shape, hooks, rendered markup, toggle state) is TDD.

---

### Task 1: Project Scaffolding

**Files:**
- Create: `package.json`
- Create: `vite.config.js`
- Create: `index.html`
- Create: `src/main.jsx`
- Create: `src/App.jsx`
- Create: `src/setupTests.js`
- Create: `src/styles/tokens.css`
- Create: `src/styles/global.css`
- Create: `.gitignore`

**Interfaces:**
- Produces: a running Vite dev server, a working `npm run test` command (Vitest + jsdom + React Testing Library configured), global CSS custom properties (`--color-brand`, `--color-bg`, `--color-black`, `--color-white`, `--color-glass`, `--font-body`, `--font-accent`, `--space-xs`, `--space-sm`, `--space-md`, `--space-lg`, `--space-xl`) usable from any component's CSS Module.

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "freelance-portfolio",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@gsap/react": "^2.1.1",
    "gsap": "^3.12.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@vitejs/plugin-react": "^4.3.2",
    "jsdom": "^25.0.1",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
```

- [ ] **Step 2: Create `vite.config.js`**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: './src/setupTests.js',
    globals: true,
  },
})
```

- [ ] **Step 3: Create `index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Alex Rivera — Brand & Web Designer</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:ital@1&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: Create `src/styles/tokens.css`**

```css
:root {
  --color-brand: #f96f39;
  --color-bg: #e9e7e3;
  --color-black: #191919;
  --color-white: #ffffff;
  --color-glass: rgba(255, 255, 255, 0.65);
  --color-glass-dark: rgba(25, 25, 25, 0.3);

  --font-body: "Inter", sans-serif;
  --font-accent: "Playfair Display", serif;

  --space-xs: 0.5rem;
  --space-sm: 1rem;
  --space-md: 2rem;
  --space-lg: 4rem;
  --space-xl: 8rem;
}
```

- [ ] **Step 5: Create `src/styles/global.css`**

```css
* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
  overflow-x: clip;
  background: var(--color-bg);
  color: var(--color-black);
  font-family: var(--font-body);
}

#root {
  min-height: 100vh;
  position: relative;
}

button {
  font-family: inherit;
}
```

- [ ] **Step 6: Create `src/setupTests.js`**

```js
import '@testing-library/jest-dom/vitest'

if (!window.matchMedia) {
  window.matchMedia = () => ({
    matches: false,
    media: '',
    addEventListener: () => {},
    removeEventListener: () => {},
  })
}
```

- [ ] **Step 7: Create `src/main.jsx`**

```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './styles/tokens.css'
import './styles/global.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 8: Create `src/App.jsx` (placeholder, fleshed out in Task 9)**

```jsx
export default function App() {
  return <div>Hero app scaffold</div>
}
```

- [ ] **Step 9: Create `.gitignore`**

```
node_modules
dist
```

- [ ] **Step 10: Install dependencies**

Run: `npm install`
Expected: installs without errors, creates `package-lock.json` and `node_modules/`.

- [ ] **Step 11: Verify the dev server**

Run: `npm run dev`
Expected: Vite starts on `http://localhost:5173`; opening it in a browser shows "Hero app scaffold" on the warm off-white (`#e9e7e3`) background. Stop the server (Ctrl+C) once confirmed.

- [ ] **Step 12: Commit**

```bash
git add package.json package-lock.json vite.config.js index.html .gitignore src/main.jsx src/App.jsx src/setupTests.js src/styles/tokens.css src/styles/global.css
git commit -m "chore: scaffold Vite + React project with Vitest configured"
```

---

### Task 2: Content Data Module + Reduced-Motion Hook

**Files:**
- Create: `src/content/site.js`
- Test: `src/content/site.test.js`
- Create: `src/hooks/usePrefersReducedMotion.js`
- Test: `src/hooks/usePrefersReducedMotion.test.js`

**Interfaces:**
- Produces: `site` object (named export from `src/content/site.js`) with shape:
  ```
  {
    name: string,
    tagline: string,
    headline: {
      lineOne: [{ type: 'text'|'image'|'accent', value?: string, alt?: string, gradient?: string }],
      lineTwo: [{ type: 'text'|'image'|'accent', value?: string, alt?: string, gradient?: string }],
    },
    actions: { primary: { label: string, href: string }, secondary: { label: string, href: string } },
    nav: [{ label: string, href: string }],
    social: [{ label: string, href: string }],
    marqueeText: string,
  }
  ```
- Produces: `usePrefersReducedMotion()` (named export from `src/hooks/usePrefersReducedMotion.js`) — returns `boolean`, updates live when the OS-level media query changes.

- [ ] **Step 1: Write the failing test for the content module**

```js
// src/content/site.test.js
import { describe, expect, test } from 'vitest'
import { site } from './site'

describe('site content', () => {
  test('has a name and tagline', () => {
    expect(typeof site.name).toBe('string')
    expect(site.name.length).toBeGreaterThan(0)
    expect(site.tagline).toContain(site.name)
  })

  test('headline lines contain at least one accent entry each', () => {
    const hasAccent = (line) => line.some((part) => part.type === 'accent')
    expect(hasAccent(site.headline.lineOne)).toBe(true)
    expect(hasAccent(site.headline.lineTwo)).toBe(true)
  })

  test('has primary and secondary actions with labels and hrefs', () => {
    expect(site.actions.primary).toMatchObject({ label: expect.any(String), href: expect.any(String) })
    expect(site.actions.secondary).toMatchObject({ label: expect.any(String), href: expect.any(String) })
  })

  test('has at least one nav item and one social link', () => {
    expect(site.nav.length).toBeGreaterThan(0)
    expect(site.social.length).toBeGreaterThan(0)
  })

  test('has marquee text', () => {
    expect(typeof site.marqueeText).toBe('string')
    expect(site.marqueeText.length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/content/site.test.js`
Expected: FAIL — `Failed to resolve import "./site"` (module doesn't exist yet).

- [ ] **Step 3: Create `src/content/site.js`**

```js
export const site = {
  name: 'Alex Rivera',
  tagline: "I'm Alex Rivera, Berlin based..",
  headline: {
    lineOne: [
      { type: 'text', value: 'BRAND & WEB' },
      {
        type: 'image',
        alt: 'Brand identity project thumbnail',
        gradient: 'linear-gradient(135deg, #f96f39, #e9e7e3)',
      },
      { type: 'accent', value: 'designer,' },
    ],
    lineTwo: [
      { type: 'text', value: 'WEBFLOW' },
      {
        type: 'image',
        alt: 'Webflow project thumbnail',
        gradient: 'linear-gradient(135deg, #191919, #f96f39)',
      },
      { type: 'accent', value: 'developer.' },
    ],
  },
  actions: {
    primary: { label: 'See My Work', href: '#work' },
    secondary: { label: 'Services', href: '#services' },
  },
  nav: [
    { label: 'Home', href: '#home' },
    { label: 'Work', href: '#work' },
    { label: 'Contact', href: '#contact' },
  ],
  social: [
    { label: 'LinkedIn', href: 'https://linkedin.com' },
    { label: 'Instagram', href: 'https://instagram.com' },
  ],
  marqueeText: 'Passion / Craft / Story',
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/content/site.test.js`
Expected: PASS (5 tests).

- [ ] **Step 5: Write the failing test for the reduced-motion hook**

```js
// src/hooks/usePrefersReducedMotion.test.js
import { act, renderHook } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { usePrefersReducedMotion } from './usePrefersReducedMotion'

function mockMatchMedia(initialMatches) {
  let listener
  const mediaQueryList = {
    matches: initialMatches,
    addEventListener: (_event, callback) => {
      listener = callback
    },
    removeEventListener: () => {},
  }
  window.matchMedia = () => mediaQueryList
  return {
    trigger(matches) {
      mediaQueryList.matches = matches
      listener({ matches })
    },
  }
}

describe('usePrefersReducedMotion', () => {
  test('returns false when the user has not requested reduced motion', () => {
    mockMatchMedia(false)
    const { result } = renderHook(() => usePrefersReducedMotion())
    expect(result.current).toBe(false)
  })

  test('returns true when the user has requested reduced motion', () => {
    mockMatchMedia(true)
    const { result } = renderHook(() => usePrefersReducedMotion())
    expect(result.current).toBe(true)
  })

  test('updates when the media query changes after mount', () => {
    const mediaQuery = mockMatchMedia(false)
    const { result } = renderHook(() => usePrefersReducedMotion())
    act(() => mediaQuery.trigger(true))
    expect(result.current).toBe(true)
  })
})
```

- [ ] **Step 6: Run test to verify it fails**

Run: `npx vitest run src/hooks/usePrefersReducedMotion.test.js`
Expected: FAIL — `Failed to resolve import "./usePrefersReducedMotion"`.

- [ ] **Step 7: Create `src/hooks/usePrefersReducedMotion.js`**

```js
import { useEffect, useState } from 'react'

const QUERY = '(prefers-reduced-motion: reduce)'

export function usePrefersReducedMotion() {
  const [prefersReduced, setPrefersReduced] = useState(
    () => window.matchMedia(QUERY).matches,
  )

  useEffect(() => {
    const mediaQueryList = window.matchMedia(QUERY)
    const listener = (event) => setPrefersReduced(event.matches)
    mediaQueryList.addEventListener('change', listener)
    return () => mediaQueryList.removeEventListener('change', listener)
  }, [])

  return prefersReduced
}
```

- [ ] **Step 8: Run test to verify it passes**

Run: `npx vitest run src/hooks/usePrefersReducedMotion.test.js`
Expected: PASS (3 tests).

- [ ] **Step 9: Commit**

```bash
git add src/content/site.js src/content/site.test.js src/hooks/usePrefersReducedMotion.js src/hooks/usePrefersReducedMotion.test.js
git commit -m "feat: add placeholder content module and reduced-motion hook"
```

---

### Task 3: SocialLinks Component

**Files:**
- Create: `src/components/SocialLinks/SocialLinks.jsx`
- Create: `src/components/SocialLinks/SocialLinks.module.css`
- Test: `src/components/SocialLinks/SocialLinks.test.jsx`

**Interfaces:**
- Consumes: `site.social` (array of `{ label: string, href: string }`) from `src/content/site.js` (Task 2).
- Produces: `SocialLinks` (named export, no props) — a fixed bottom-left cluster of icon links, importable as `import { SocialLinks } from '../components/SocialLinks/SocialLinks'`.

- [ ] **Step 1: Write the failing test**

```jsx
// src/components/SocialLinks/SocialLinks.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { SocialLinks } from './SocialLinks'
import { site } from '../../content/site'

describe('SocialLinks', () => {
  test('renders a link for every social entry with the correct href', () => {
    render(<SocialLinks />)
    site.social.forEach(({ label, href }) => {
      expect(screen.getByRole('link', { name: label })).toHaveAttribute('href', href)
    })
  })

  test('opens links in a new tab safely', () => {
    render(<SocialLinks />)
    const link = screen.getByRole('link', { name: site.social[0].label })
    expect(link).toHaveAttribute('target', '_blank')
    expect(link).toHaveAttribute('rel', expect.stringContaining('noopener'))
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/SocialLinks/SocialLinks.test.jsx`
Expected: FAIL — `Failed to resolve import "./SocialLinks"`.

- [ ] **Step 3: Create `src/components/SocialLinks/SocialLinks.module.css`**

```css
.socialLinks {
  position: fixed;
  left: var(--space-md);
  bottom: var(--space-md);
  display: flex;
  gap: var(--space-xs);
  z-index: 10;
}

.link {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 50%;
  border: 1px solid var(--color-black);
  color: var(--color-black);
  text-decoration: none;
  font-size: 0.75rem;
  font-weight: 600;
}

.link:hover {
  background: var(--color-black);
  color: var(--color-white);
}
```

- [ ] **Step 4: Create `src/components/SocialLinks/SocialLinks.jsx`**

```jsx
import { site } from '../../content/site'
import styles from './SocialLinks.module.css'

export function SocialLinks() {
  return (
    <div className={styles.socialLinks}>
      {site.social.map(({ label, href }) => (
        <a
          key={label}
          href={href}
          target="_blank"
          rel="noopener noreferrer"
          className={styles.link}
          aria-label={label}
        >
          {label.slice(0, 2)}
        </a>
      ))}
    </div>
  )
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run src/components/SocialLinks/SocialLinks.test.jsx`
Expected: PASS (2 tests).

- [ ] **Step 6: Commit**

```bash
git add src/components/SocialLinks
git commit -m "feat: add SocialLinks component"
```

---

### Task 4: GSAP Setup + ScrollCue Component

**Files:**
- Create: `src/lib/gsap.js`
- Create: `src/components/ScrollCue/ScrollCue.jsx`
- Create: `src/components/ScrollCue/ScrollCue.module.css`
- Test: `src/components/ScrollCue/ScrollCue.test.jsx`

**Interfaces:**
- Consumes: `usePrefersReducedMotion` from `src/hooks/usePrefersReducedMotion.js` (Task 2).
- Produces: `gsap`, `ScrollTrigger` (named exports from `src/lib/gsap.js`, plugin already registered) for use by every later animated component. Produces `ScrollCue` (named export, no props) — a fixed bottom-right scroll-down button.

- [ ] **Step 1: Create `src/lib/gsap.js`**

This is one-line plugin registration with no independently testable behavior of its own — it's infrastructure the ScrollCue component (and every later animated component) depends on, so it's created here rather than as its own task.

```js
import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger'

gsap.registerPlugin(ScrollTrigger)

export { gsap, ScrollTrigger }
```

- [ ] **Step 2: Write the failing test for ScrollCue**

```jsx
// src/components/ScrollCue/ScrollCue.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test, vi } from 'vitest'
import { ScrollCue } from './ScrollCue'

describe('ScrollCue', () => {
  test('renders a labeled scroll-down button', () => {
    render(<ScrollCue />)
    expect(screen.getByRole('button', { name: 'Scroll down' })).toBeInTheDocument()
  })

  test('scrolls to one viewport height on click', () => {
    window.scrollTo = vi.fn()
    render(<ScrollCue />)
    screen.getByRole('button', { name: 'Scroll down' }).click()
    expect(window.scrollTo).toHaveBeenCalledWith({ top: window.innerHeight, behavior: 'smooth' })
  })
})
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run src/components/ScrollCue/ScrollCue.test.jsx`
Expected: FAIL — `Failed to resolve import "./ScrollCue"`.

- [ ] **Step 4: Create `src/components/ScrollCue/ScrollCue.module.css`**

```css
.scrollCue {
  position: fixed;
  right: var(--space-md);
  bottom: var(--space-md);
  width: 3rem;
  height: 3rem;
  border-radius: 50%;
  border: 1px solid var(--color-black);
  background: transparent;
  color: var(--color-black);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: 10;
}

.scrollCue:hover {
  background: var(--color-black);
  color: var(--color-white);
}
```

- [ ] **Step 5: Create `src/components/ScrollCue/ScrollCue.jsx`**

```jsx
import { useRef } from 'react'
import { useGSAP } from '@gsap/react'
import { gsap } from '../../lib/gsap'
import { usePrefersReducedMotion } from '../../hooks/usePrefersReducedMotion'
import styles from './ScrollCue.module.css'

export function ScrollCue() {
  const buttonRef = useRef(null)
  const prefersReducedMotion = usePrefersReducedMotion()

  useGSAP(() => {
    if (prefersReducedMotion) return
    const tween = gsap.to(buttonRef.current, {
      y: 8,
      duration: 0.9,
      ease: 'sine.inOut',
      yoyo: true,
      repeat: -1,
    })
    return () => tween.kill()
  }, [prefersReducedMotion])

  const handleClick = () => {
    window.scrollTo({ top: window.innerHeight, behavior: 'smooth' })
  }

  return (
    <button
      ref={buttonRef}
      type="button"
      className={styles.scrollCue}
      aria-label="Scroll down"
      onClick={handleClick}
    >
      <svg viewBox="0 0 24 24" width="18" height="18" aria-hidden="true">
        <path
          d="M12 4v16m0 0l-6-6m6 6l6-6"
          stroke="currentColor"
          strokeWidth="2"
          fill="none"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
      </svg>
    </button>
  )
}
```

- [ ] **Step 6: Run test to verify it passes**

Run: `npx vitest run src/components/ScrollCue/ScrollCue.test.jsx`
Expected: PASS (2 tests).

- [ ] **Step 7: Manual verification — bounce animation**

Temporarily render `<ScrollCue />` in `src/App.jsx`, run `npm run dev`, and confirm in the browser that the button gently bounces up and down in a continuous loop. Then enable OS-level "reduce motion" (Windows: Settings → Accessibility → Visual effects → Animation effects, off) and confirm the bounce stops. Revert the temporary `App.jsx` change (it gets rebuilt properly in Task 9).

- [ ] **Step 8: Commit**

```bash
git add src/lib/gsap.js src/components/ScrollCue
git commit -m "feat: add gsap setup and ScrollCue component"
```

---

### Task 5: HeroActions Component

**Files:**
- Create: `src/components/Hero/HeroActions.jsx`
- Create: `src/components/Hero/HeroActions.module.css`
- Test: `src/components/Hero/HeroActions.test.jsx`

**Interfaces:**
- Consumes: `site.actions` (`{ primary: { label, href }, secondary: { label, href } }`) from `src/content/site.js` (Task 2).
- Produces: `HeroActions` (named export, no props), importable as `import { HeroActions } from './HeroActions'`.

- [ ] **Step 1: Write the failing test**

```jsx
// src/components/Hero/HeroActions.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { HeroActions } from './HeroActions'
import { site } from '../../content/site'

describe('HeroActions', () => {
  test('renders the primary and secondary CTAs with correct hrefs', () => {
    render(<HeroActions />)
    expect(screen.getByRole('link', { name: site.actions.primary.label })).toHaveAttribute(
      'href',
      site.actions.primary.href,
    )
    expect(screen.getByRole('link', { name: site.actions.secondary.label })).toHaveAttribute(
      'href',
      site.actions.secondary.href,
    )
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/Hero/HeroActions.test.jsx`
Expected: FAIL — `Failed to resolve import "./HeroActions"`.

- [ ] **Step 3: Create `src/components/Hero/HeroActions.module.css`**

```css
.actions {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
}

.primary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.9rem 1.5rem;
  border-radius: 999px;
  background: var(--color-black);
  color: var(--color-white);
  text-decoration: none;
  font-weight: 600;
}

.secondary {
  display: inline-flex;
  align-items: center;
  padding: 0.9rem 1.5rem;
  border-radius: 999px;
  border: 1px solid var(--color-black);
  color: var(--color-black);
  text-decoration: none;
  font-weight: 500;
}
```

- [ ] **Step 4: Create `src/components/Hero/HeroActions.jsx`**

```jsx
import { site } from '../../content/site'
import styles from './HeroActions.module.css'

export function HeroActions() {
  return (
    <div className={styles.actions}>
      <a className={styles.primary} href={site.actions.primary.href}>
        {site.actions.primary.label}
      </a>
      <a className={styles.secondary} href={site.actions.secondary.href}>
        {site.actions.secondary.label}
      </a>
    </div>
  )
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run src/components/Hero/HeroActions.test.jsx`
Expected: PASS (1 test).

- [ ] **Step 6: Commit**

```bash
git add src/components/Hero/HeroActions.jsx src/components/Hero/HeroActions.module.css src/components/Hero/HeroActions.test.jsx
git commit -m "feat: add HeroActions component"
```

---

### Task 6: HeroTagline + HeroHeadline Components

**Files:**
- Create: `src/components/Hero/HeroTagline.jsx`
- Create: `src/components/Hero/HeroTagline.module.css`
- Test: `src/components/Hero/HeroTagline.test.jsx`
- Create: `src/components/Hero/HeroHeadline.jsx`
- Create: `src/components/Hero/HeroHeadline.module.css`
- Test: `src/components/Hero/HeroHeadline.test.jsx`

**Interfaces:**
- Consumes: `site.tagline` (string) and `site.headline.{lineOne,lineTwo}` (arrays of `{ type, value?, alt?, gradient? }`) from `src/content/site.js` (Task 2). Consumes `gsap` from `src/lib/gsap.js` and `usePrefersReducedMotion` (Tasks 2, 4).
- Produces: `HeroTagline`, `HeroHeadline` (named exports, no props), each exposing a root element with `data-hero-tagline` / `data-hero-headline` attributes for later GSAP targeting via `useGSAP`'s scope.

- [ ] **Step 1: Write the failing test for HeroTagline**

```jsx
// src/components/Hero/HeroTagline.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { HeroTagline } from './HeroTagline'
import { site } from '../../content/site'

describe('HeroTagline', () => {
  test('renders the tagline text', () => {
    render(<HeroTagline />)
    expect(screen.getByText(site.tagline)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/Hero/HeroTagline.test.jsx`
Expected: FAIL — `Failed to resolve import "./HeroTagline"`.

- [ ] **Step 3: Create `src/components/Hero/HeroTagline.module.css`**

```css
.tagline {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.4rem 0.9rem;
  border-radius: 999px;
  background: var(--color-white);
  font-size: 0.875rem;
  margin-bottom: var(--space-sm);
}

.dot {
  width: 0.5rem;
  height: 0.5rem;
  border-radius: 50%;
  background: var(--color-brand);
  flex-shrink: 0;
}
```

- [ ] **Step 4: Create `src/components/Hero/HeroTagline.jsx`**

```jsx
import { site } from '../../content/site'
import styles from './HeroTagline.module.css'

export function HeroTagline() {
  return (
    <p className={styles.tagline} data-hero-tagline>
      <span className={styles.dot} aria-hidden="true" />
      {site.tagline}
    </p>
  )
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run src/components/Hero/HeroTagline.test.jsx`
Expected: PASS (1 test).

- [ ] **Step 6: Write the failing test for HeroHeadline**

```jsx
// src/components/Hero/HeroHeadline.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { HeroHeadline } from './HeroHeadline'
import { site } from '../../content/site'

describe('HeroHeadline', () => {
  test('renders every text and accent part from both headline lines', () => {
    render(<HeroHeadline />)
    ;[...site.headline.lineOne, ...site.headline.lineTwo]
      .filter((part) => part.type !== 'image')
      .forEach((part) => {
        expect(screen.getByText(part.value)).toBeInTheDocument()
      })
  })

  test('renders an accessible placeholder for every image part', () => {
    render(<HeroHeadline />)
    ;[...site.headline.lineOne, ...site.headline.lineTwo]
      .filter((part) => part.type === 'image')
      .forEach((part) => {
        expect(screen.getByRole('img', { name: part.alt })).toBeInTheDocument()
      })
  })
})
```

- [ ] **Step 7: Run test to verify it fails**

Run: `npx vitest run src/components/Hero/HeroHeadline.test.jsx`
Expected: FAIL — `Failed to resolve import "./HeroHeadline"`.

- [ ] **Step 8: Create `src/components/Hero/HeroHeadline.module.css`**

```css
.headline {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  font-size: clamp(2.5rem, 6vw, 5.5rem);
  font-weight: 700;
  line-height: 1;
  text-transform: uppercase;
  margin: 0 0 var(--space-md);
}

.line {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.thumb {
  display: inline-block;
  width: 3.5em;
  height: 1.7em;
  border-radius: 0.75rem;
  vertical-align: middle;
}

.accent {
  font-family: var(--font-accent);
  font-style: italic;
  font-weight: 400;
  text-transform: none;
  color: var(--color-brand);
}
```

- [ ] **Step 9: Create `src/components/Hero/HeroHeadline.jsx`**

```jsx
import { site } from '../../content/site'
import styles from './HeroHeadline.module.css'

function HeadlinePart({ part }) {
  if (part.type === 'image') {
    return (
      <span
        role="img"
        aria-label={part.alt}
        className={styles.thumb}
        style={{ background: part.gradient }}
      />
    )
  }
  if (part.type === 'accent') {
    return <span className={styles.accent}>{part.value}</span>
  }
  return <span>{part.value}</span>
}

export function HeroHeadline() {
  return (
    <h1 className={styles.headline} data-hero-headline>
      <span className={styles.line}>
        {site.headline.lineOne.map((part, index) => (
          <HeadlinePart key={index} part={part} />
        ))}
      </span>
      <span className={styles.line}>
        {site.headline.lineTwo.map((part, index) => (
          <HeadlinePart key={index} part={part} />
        ))}
      </span>
    </h1>
  )
}
```

- [ ] **Step 10: Run test to verify it passes**

Run: `npx vitest run src/components/Hero/HeroHeadline.test.jsx`
Expected: PASS (2 tests).

- [ ] **Step 11: Add the load-in stagger and scroll parallax animation**

Modify `src/components/Hero/HeroHeadline.jsx` to add a GSAP load-in timeline and a scroll-linked parallax on the thumbnail placeholders:

```jsx
import { useRef } from 'react'
import { useGSAP } from '@gsap/react'
import { gsap } from '../../lib/gsap'
import { usePrefersReducedMotion } from '../../hooks/usePrefersReducedMotion'
import { site } from '../../content/site'
import styles from './HeroHeadline.module.css'

function HeadlinePart({ part }) {
  if (part.type === 'image') {
    return (
      <span
        role="img"
        aria-label={part.alt}
        className={styles.thumb}
        data-hero-thumb
        style={{ background: part.gradient }}
      />
    )
  }
  if (part.type === 'accent') {
    return <span className={styles.accent}>{part.value}</span>
  }
  return <span>{part.value}</span>
}

export function HeroHeadline() {
  const rootRef = useRef(null)
  const prefersReducedMotion = usePrefersReducedMotion()

  useGSAP(
    () => {
      const lines = rootRef.current.querySelectorAll(`.${styles.line}`)
      gsap.from(lines, {
        y: 40,
        opacity: 0,
        duration: 0.8,
        stagger: 0.15,
        ease: 'power3.out',
      })

      if (prefersReducedMotion) return
      const thumbs = rootRef.current.querySelectorAll('[data-hero-thumb]')
      thumbs.forEach((thumb, index) => {
        gsap.to(thumb, {
          yPercent: index % 2 === 0 ? 20 : -20,
          ease: 'none',
          scrollTrigger: {
            trigger: rootRef.current,
            start: 'top bottom',
            end: 'bottom top',
            scrub: true,
          },
        })
      })
    },
    { scope: rootRef, dependencies: [prefersReducedMotion] },
  )

  return (
    <h1 className={styles.headline} data-hero-headline ref={rootRef}>
      <span className={styles.line}>
        {site.headline.lineOne.map((part, index) => (
          <HeadlinePart key={index} part={part} />
        ))}
      </span>
      <span className={styles.line}>
        {site.headline.lineTwo.map((part, index) => (
          <HeadlinePart key={index} part={part} />
        ))}
      </span>
    </h1>
  )
}
```

- [ ] **Step 12: Run the HeroHeadline test suite again to confirm the animation didn't break the structural test**

Run: `npx vitest run src/components/Hero/HeroHeadline.test.jsx`
Expected: PASS (2 tests) — jsdom runs the GSAP `.from()`/ScrollTrigger setup without throwing; it doesn't assert on the animation itself.

- [ ] **Step 13: Manual verification — load-in and parallax**

Temporarily render `<HeroHeadline />` inside `src/App.jsx`, run `npm run dev`, and confirm: (1) both headline lines fade/slide up on load, staggered; (2) scrolling the page moves the two gradient thumbnails at slightly different speeds than the surrounding text. Toggle OS "reduce motion" and confirm the parallax stops while the load-in fade still plays (load-in is a one-shot entrance, not a loop, so it's fine to leave enabled). Revert the temporary `App.jsx` change.

- [ ] **Step 14: Commit**

```bash
git add src/components/Hero/HeroTagline.jsx src/components/Hero/HeroTagline.module.css src/components/Hero/HeroTagline.test.jsx src/components/Hero/HeroHeadline.jsx src/components/Hero/HeroHeadline.module.css src/components/Hero/HeroHeadline.test.jsx
git commit -m "feat: add HeroTagline and animated HeroHeadline components"
```

---

### Task 7: BackgroundMarquee Component

**Files:**
- Create: `src/components/BackgroundMarquee/BackgroundMarquee.jsx`
- Create: `src/components/BackgroundMarquee/BackgroundMarquee.module.css`
- Test: `src/components/BackgroundMarquee/BackgroundMarquee.test.jsx`

**Interfaces:**
- Consumes: `site.marqueeText` (string) from `src/content/site.js` (Task 2). Consumes `gsap` and `usePrefersReducedMotion` (Tasks 2, 4).
- Produces: `BackgroundMarquee` (named export, no props).

- [ ] **Step 1: Write the failing test**

```jsx
// src/components/BackgroundMarquee/BackgroundMarquee.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { BackgroundMarquee } from './BackgroundMarquee'
import { site } from '../../content/site'

describe('BackgroundMarquee', () => {
  test('is hidden from assistive tech and repeats the marquee text for a seamless loop', () => {
    render(<BackgroundMarquee />)
    const track = screen.getByTestId('marquee-track')
    expect(track).toHaveAttribute('aria-hidden', 'true')
    expect(screen.getAllByText(site.marqueeText)).toHaveLength(2)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/BackgroundMarquee/BackgroundMarquee.test.jsx`
Expected: FAIL — `Failed to resolve import "./BackgroundMarquee"`.

- [ ] **Step 3: Create `src/components/BackgroundMarquee/BackgroundMarquee.module.css`**

```css
.marquee {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
  pointer-events: none;
  z-index: 0;
}

.track {
  display: flex;
  width: max-content;
  white-space: nowrap;
  font-size: clamp(4rem, 14vw, 12rem);
  font-weight: 700;
  color: transparent;
  -webkit-text-stroke: 1px rgba(25, 25, 25, 0.15);
}

.track span {
  padding-right: 2rem;
}
```

- [ ] **Step 4: Create `src/components/BackgroundMarquee/BackgroundMarquee.jsx`**

```jsx
import { useRef } from 'react'
import { useGSAP } from '@gsap/react'
import { gsap } from '../../lib/gsap'
import { usePrefersReducedMotion } from '../../hooks/usePrefersReducedMotion'
import { site } from '../../content/site'
import styles from './BackgroundMarquee.module.css'

export function BackgroundMarquee() {
  const trackRef = useRef(null)
  const prefersReducedMotion = usePrefersReducedMotion()

  useGSAP(
    () => {
      if (prefersReducedMotion) return
      const tween = gsap.to(trackRef.current, {
        xPercent: -50,
        duration: 20,
        ease: 'none',
        repeat: -1,
      })
      return () => tween.kill()
    },
    { dependencies: [prefersReducedMotion] },
  )

  return (
    <div className={styles.marquee}>
      <div className={styles.track} ref={trackRef} data-testid="marquee-track" aria-hidden="true">
        <span>{site.marqueeText}</span>
        <span>{site.marqueeText}</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run src/components/BackgroundMarquee/BackgroundMarquee.test.jsx`
Expected: PASS (1 test).

- [ ] **Step 6: Manual verification — infinite loop**

Temporarily render `<BackgroundMarquee />` in `src/App.jsx`, run `npm run dev`, and confirm the text scrolls left continuously with no visible jump at the loop point, and no horizontal scrollbar appears on the page. Toggle OS "reduce motion" and confirm the loop stops (text stays static). Revert the temporary `App.jsx` change.

- [ ] **Step 7: Commit**

```bash
git add src/components/BackgroundMarquee
git commit -m "feat: add BackgroundMarquee component"
```

---

### Task 8: Navbar + NavCard

**Files:**
- Create: `src/components/Navbar/Navbar.jsx`
- Create: `src/components/Navbar/Navbar.module.css`
- Create: `src/components/Navbar/NavCard.jsx`
- Create: `src/components/Navbar/NavCard.module.css`
- Test: `src/components/Navbar/Navbar.test.jsx`

**Interfaces:**
- Consumes: `site.name` (string) and `site.nav` (array of `{ label, href }`) from `src/content/site.js` (Task 2). Consumes `gsap`, `usePrefersReducedMotion` (Tasks 2, 4).
- Produces: `Navbar` (named export, no props). `NavCard` (named export, props: `{ label: string, href: string }`) used internally by `Navbar`, not consumed elsewhere.

- [ ] **Step 1: Write the failing test**

```jsx
// src/components/Navbar/Navbar.test.jsx
import { render, screen } from '@testing-library/react'
import { describe, expect, test } from 'vitest'
import { Navbar } from './Navbar'
import { site } from '../../content/site'

describe('Navbar', () => {
  test('starts closed with nav items hidden from assistive tech', () => {
    render(<Navbar />)
    const toggle = screen.getByRole('button', { name: site.name })
    expect(toggle).toHaveAttribute('aria-expanded', 'false')
    site.nav.forEach(({ label }) => {
      expect(screen.getByRole('link', { name: label, hidden: true })).toHaveAttribute(
        'aria-hidden',
        'true',
      )
    })
  })

  test('clicking the toggle opens the menu and reveals nav links', () => {
    render(<Navbar />)
    const toggle = screen.getByRole('button', { name: site.name })
    toggle.click()
    expect(toggle).toHaveAttribute('aria-expanded', 'true')
    site.nav.forEach(({ label, href }) => {
      const link = screen.getByRole('link', { name: label })
      expect(link).toHaveAttribute('aria-hidden', 'false')
      expect(link).toHaveAttribute('href', href)
    })
  })

  test('clicking the toggle again closes the menu', () => {
    render(<Navbar />)
    const toggle = screen.getByRole('button', { name: site.name })
    toggle.click()
    toggle.click()
    expect(toggle).toHaveAttribute('aria-expanded', 'false')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/Navbar/Navbar.test.jsx`
Expected: FAIL — `Failed to resolve import "./Navbar"`.

- [ ] **Step 3: Create `src/components/Navbar/NavCard.module.css`**

```css
.card {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 8rem;
  height: 5rem;
  border-radius: 1rem;
  background: var(--color-white);
  color: var(--color-black);
  text-decoration: none;
  font-weight: 600;
  box-shadow: 0 10px 30px rgba(25, 25, 25, 0.15);
}
```

- [ ] **Step 4: Create `src/components/Navbar/NavCard.jsx`**

```jsx
import styles from './NavCard.module.css'

export function NavCard({ label, href, isOpen, cardRef }) {
  return (
    <a
      ref={cardRef}
      href={href}
      className={styles.card}
      aria-hidden={!isOpen}
      tabIndex={isOpen ? 0 : -1}
    >
      {label}
    </a>
  )
}
```

- [ ] **Step 5: Create `src/components/Navbar/Navbar.module.css`**

```css
.navbar {
  position: fixed;
  top: var(--space-md);
  left: 50%;
  transform: translateX(-50%);
  z-index: 20;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.toggle {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  padding: 0.6rem 1.2rem;
  border-radius: 999px;
  border: none;
  background: var(--color-glass);
  backdrop-filter: blur(8px);
  color: var(--color-black);
  font-weight: 600;
  cursor: pointer;
}

.monogram {
  width: 1.75rem;
  height: 1.75rem;
  border-radius: 50%;
  background: var(--color-brand);
  color: var(--color-white);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.85rem;
}

.backdrop {
  position: fixed;
  inset: 0;
  background: rgba(25, 25, 25, 0.2);
  backdrop-filter: blur(6px);
  opacity: 0;
  pointer-events: none;
  z-index: -1;
}

.cards {
  position: fixed;
  top: 6rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: var(--space-sm);
}
```

- [ ] **Step 6: Create `src/components/Navbar/Navbar.jsx`**

```jsx
import { useRef, useState } from 'react'
import { site } from '../../content/site'
import { NavCard } from './NavCard'
import styles from './Navbar.module.css'

export function Navbar() {
  const [isOpen, setIsOpen] = useState(false)
  const cardRefs = useRef([])

  const toggle = () => setIsOpen((open) => !open)

  return (
    <nav className={styles.navbar}>
      <button
        type="button"
        className={styles.toggle}
        aria-expanded={isOpen}
        aria-label={site.name}
        onClick={toggle}
      >
        <span className={styles.monogram} aria-hidden="true">
          {site.name.charAt(0)}
        </span>
        {site.name}
      </button>
      <div className={styles.backdrop} data-open={isOpen} />
      <div className={styles.cards}>
        {site.nav.map((item, index) => (
          <NavCard
            key={item.label}
            label={item.label}
            href={item.href}
            isOpen={isOpen}
            cardRef={(el) => (cardRefs.current[index] = el)}
          />
        ))}
      </div>
    </nav>
  )
}
```

- [ ] **Step 7: Run test to verify it passes**

Run: `npx vitest run src/components/Navbar/Navbar.test.jsx`
Expected: PASS (3 tests).

- [ ] **Step 8: Add the scatter-open GSAP animation**

Modify `src/components/Navbar/Navbar.jsx` to animate the backdrop and cards with `useGSAP`, keyed on `isOpen`:

```jsx
import { useRef, useState } from 'react'
import { useGSAP } from '@gsap/react'
import { gsap } from '../../lib/gsap'
import { usePrefersReducedMotion } from '../../hooks/usePrefersReducedMotion'
import { site } from '../../content/site'
import { NavCard } from './NavCard'
import styles from './Navbar.module.css'

export function Navbar() {
  const [isOpen, setIsOpen] = useState(false)
  const cardRefs = useRef([])
  const backdropRef = useRef(null)
  const rootRef = useRef(null)
  const prefersReducedMotion = usePrefersReducedMotion()

  const toggle = () => setIsOpen((open) => !open)

  useGSAP(
    () => {
      const cards = cardRefs.current.filter(Boolean)
      if (prefersReducedMotion) {
        gsap.set(backdropRef.current, { opacity: isOpen ? 1 : 0, pointerEvents: isOpen ? 'auto' : 'none' })
        gsap.set(cards, { opacity: isOpen ? 1 : 0, scale: 1, rotate: 0 })
        return
      }

      if (isOpen) {
        gsap.to(backdropRef.current, { opacity: 1, pointerEvents: 'auto', duration: 0.3 })
        gsap.fromTo(
          cards,
          { opacity: 0, scale: 0.6, rotate: 0 },
          {
            opacity: 1,
            scale: 1,
            rotate: () => gsap.utils.random(-10, 10),
            duration: 0.5,
            stagger: 0.08,
            ease: 'back.out(1.7)',
          },
        )
      } else {
        gsap.to(backdropRef.current, { opacity: 0, pointerEvents: 'none', duration: 0.3 })
        gsap.to(cards, {
          opacity: 0,
          scale: 0.6,
          rotate: () => gsap.utils.random(-24, 24),
          duration: 0.35,
          stagger: 0.05,
          ease: 'power2.in',
        })
      }
    },
    { scope: rootRef, dependencies: [isOpen, prefersReducedMotion] },
  )

  return (
    <nav className={styles.navbar} ref={rootRef}>
      <button
        type="button"
        className={styles.toggle}
        aria-expanded={isOpen}
        aria-label={site.name}
        onClick={toggle}
      >
        <span className={styles.monogram} aria-hidden="true">
          {site.name.charAt(0)}
        </span>
        {site.name}
      </button>
      <div className={styles.backdrop} ref={backdropRef} />
      <div className={styles.cards}>
        {site.nav.map((item, index) => (
          <NavCard
            key={item.label}
            label={item.label}
            href={item.href}
            isOpen={isOpen}
            cardRef={(el) => (cardRefs.current[index] = el)}
          />
        ))}
      </div>
    </nav>
  )
}
```

- [ ] **Step 9: Run the Navbar test suite again to confirm the animation didn't break the structural tests**

Run: `npx vitest run src/components/Navbar/Navbar.test.jsx`
Expected: PASS (3 tests).

- [ ] **Step 10: Manual verification — scatter open/close**

Temporarily render `<Navbar />` in `src/App.jsx`, run `npm run dev`, and confirm: clicking the pill blurs the backdrop and scatters the 3 cards in with randomized rotation and a stagger; clicking again scatters them back out. Toggle OS "reduce motion" and confirm the menu still opens/closes but instantly, with no blur/scatter motion. Revert the temporary `App.jsx` change.

- [ ] **Step 11: Commit**

```bash
git add src/components/Navbar
git commit -m "feat: add Navbar with animated nav-card scatter menu"
```

---

### Task 9: Assemble the Hero Page

**Files:**
- Modify: `src/App.jsx`
- Create: `src/App.module.css`

**Interfaces:**
- Consumes: `Navbar` (Task 8), `HeroTagline`, `HeroHeadline`, `HeroActions` (Tasks 5, 6), `BackgroundMarquee` (Task 7), `SocialLinks` (Task 3), `ScrollCue` (Task 4).
- Produces: the assembled hero page rendered at `/`.

- [ ] **Step 1: Create `src/App.module.css`**

```css
.page {
  position: relative;
  min-height: 100vh;
  overflow: hidden;
}

.hero {
  position: relative;
  z-index: 1;
  max-width: 72rem;
  margin: 0 auto;
  padding: 10rem var(--space-md) var(--space-xl);
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}
```

- [ ] **Step 2: Rewrite `src/App.jsx` to assemble all components**

```jsx
import { Navbar } from './components/Navbar/Navbar'
import { HeroTagline } from './components/Hero/HeroTagline'
import { HeroHeadline } from './components/Hero/HeroHeadline'
import { HeroActions } from './components/Hero/HeroActions'
import { BackgroundMarquee } from './components/BackgroundMarquee/BackgroundMarquee'
import { SocialLinks } from './components/SocialLinks/SocialLinks'
import { ScrollCue } from './components/ScrollCue/ScrollCue'
import styles from './App.module.css'

export default function App() {
  return (
    <div className={styles.page}>
      <Navbar />
      <section className={styles.hero}>
        <HeroTagline />
        <HeroHeadline />
        <HeroActions />
      </section>
      <BackgroundMarquee />
      <SocialLinks />
      <ScrollCue />
    </div>
  )
}
```

- [ ] **Step 3: Run the full test suite**

Run: `npm run test`
Expected: PASS — every test file from Tasks 2–8 passes together (no cross-component interference).

- [ ] **Step 4: Full manual verification pass**

Run `npm run dev` and, in the browser, work through the spec's verification checklist:

1. At ~1900px width, confirm the layout's proportions roughly match the reference (pill nav centered top, tagline → headline → CTAs stacked left-aligned, marquee low-opacity behind everything near the bottom, social icons bottom-left, scroll cue bottom-right).
2. Resize down to a mobile width (~390px) and confirm the layout reflows without overlapping elements or unreadable text.
3. Confirm all animations fire together on the assembled page: headline load-in stagger, nav-card scatter open/close, thumbnail parallax on scroll, marquee loop, scroll-cue bounce.
4. Toggle OS "reduce motion" and confirm the marquee, bounce, parallax, and nav scatter all stop while content stays fully visible and usable.
5. Confirm there is no horizontal scrollbar at any viewport width.

- [ ] **Step 5: Commit**

```bash
git add src/App.jsx src/App.module.css
git commit -m "feat: assemble hero page from navbar, hero, marquee, social, and scroll-cue components"
```
